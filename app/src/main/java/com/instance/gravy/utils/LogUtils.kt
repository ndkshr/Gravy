package com.instance.gravy.utils

import java.lang.Exception

class LogUtils {

    companion object {
        fun logException(e: Exception) {
            // TODO: Will add logging exceptions to firebase crashlytics
        }
    }
}